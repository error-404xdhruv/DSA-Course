#include <bits/stdc++.h>
#define l long long
#define vl vector<long long> 
#define vi vector<int> 
using namespace std ;

int main()
{
    l t ;
    cin>> t ;
    while (t--)
    {
        l n;
        cin >> n ;
        vector <vector<l>> v ;
        for (l i = 0; i < n; i++)
        {
            for (l j = 0; j < 2; j++)
            {
                cin >> v[i][j] ;
            }    
        }
        l L, P;
        cin >> L >> P ;
    }
    

    return 0;
}