#include <bits/stdc++.h>
#define l long long
using namespace std;

int main()
{
    int arr1[10][10];
    int row, col;
    cin >> row >> col;
    for (int i = 0; i < row; i++)
    {
        for (int j = 0; j < col; j++)
        {
            cin >> arr1[row][col];
        }
    }

    int target ;
    cin >> target ;
    

    return 0;
}